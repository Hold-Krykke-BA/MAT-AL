package Part1.interfaces;

/**
 * Implement a directed graph for flight routes.
 *
 * The graph representation should be such that it
 * is both efficient in terms of memory use and time taken to do 5 operations.
 *
 * (Airline -> Airport, breadth-first, depth-first)
 *
 * Shortest path (distance) (Dijkstra's algorithm)
 *
 * Shortest path (time) (location -> location) (transfer: 1 hour)
 *
 * Defend choice of data structure (time & space complexity)
 */
public interface Graph {
}
