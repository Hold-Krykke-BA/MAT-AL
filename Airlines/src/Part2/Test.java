package Part2;

public class Test {
    public static void main(String[] args) {
        System.out.println("Hello World!");

        /*
        Use your own implementation of the A* algorithm to
         find the shortest path between the ’S’ character and the ’T’ character
          in the three files.
         */

        /*
        In the txt files ’S’ represents the start node,
        ’T’ represents the target node,
        ’W’ represents an impassable wall node and
        ’*’ represents a normal passable node.
        The maps are always rectangular,
        with the same number of characters on each line.
         */
    }
}
